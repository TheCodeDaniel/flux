# Building

### Flutter - default (APK)

flux build

### Flutter - build AAB

flux build --release-type aab

### React Native - build APK

flux build --release-type apk

### React Native - build AAB

flux build --release-type aab
